{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "echo Frontend build lives in ../frontend — run: cd ../frontend && npm run build",
        "dev": "echo Frontend Vite lives in ../frontend — run: cd ../frontend && npm run dev"
    },
    "devDependencies": {}
}
